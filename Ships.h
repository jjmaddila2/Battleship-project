#pragma once
#include <iostream>
#include <vector>
#include <map>
#include <string>
#include <fstream>


class Ships {
public:
	Ships();
	Ships(int len);
	int getLength();
	int length;
};
